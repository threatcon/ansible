# Loop through the sizes and use the slice filter to get the corresponding sublist
- name: Create sublist
  set_fact:
    sublists: "{{ sublists + [list | slice(item) | list] }}"
  loop: "{{ sizes }}"

# Print the result
- name: Print sublist
  debug:
    var: sublists
